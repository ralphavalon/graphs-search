package com.graphs.commands;

import com.graphs.domain.Graph;

public class DistanceCommand implements Command {

	@Override
	public String execute(Graph graph, Object... params) {
		return "9";
	}

}